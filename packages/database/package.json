{
  "name": "database",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate --accept-data-loss",
    "db:seed": "tsx seed.ts"
  },
  "dependencies": {
    "@prisma/client": "5.5.2"
  },
  "devDependencies": {
    "tsx": "^3.12.2",
    "prisma": "5.5.2"
  }
}